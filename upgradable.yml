- name: List
  hosts: "{{ group }}"
  remote_user: gooroom
  tasks:
  - name: 1. Upgradable
    become: yes
    become_method: sudo
    command: apt-get upgrade -s
    register: out

  - debug: var=out.stdout_lines
